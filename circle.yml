checkout:
  post:
    - git submodule sync
    - git submodule update --init
    - mvn clean
    - mvn package
    - cp -R ./target/MobPlugin-0.0.2-SNAPSHOT.jar $CIRCLE_ARTIFACTS

machine:
  java:
    version: oraclejdk8
